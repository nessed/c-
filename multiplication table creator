#include <iostream> 
using namespace std;
int main() {
  int table = 6; //defining up to what

  for(int i = 1; i<=table;i++){ //look which will serve as the row/number which table we are running
    for(int j = 1;j<=10;j++){ //will serve as the second part of multiplication or column aka will run 10 times each time i runs
      cout<<i<<" * "<<j<<" = "<<i*j<<'\n';
    }
  cout<<'\n';;
  }
return 0; }
